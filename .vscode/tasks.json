{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cd build && cmake .. && make -j && cd .."
        },
        {
            "label": "build_gpu",
            "type": "shell",
            "command": "cd build_gpu && cmake .. && make -j && cd .."
        },
        {
            "label": "build_cpu",
            "type": "shell",
            "command": "cd build_cpu && cmake .. && make -j && cd .."
        }
    ]
}